{
  "name": "default",
  "version": "1.0.0",
  "description": "Default Project",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently --kill-others \"cd backend && npm run dev\" \"cd frontend && npm run start\"",
    "install": "cd backend & npm install & cd .. & cd frontend & npm i",
    "build": "cd backend && npm i && npm run build && cd .. && cd frontend && npm run build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/devkalelbranco/xlearn.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/devkalelbranco/xlearn/issues"
  },
  "homepage": "https://github.com/devkalelbranco/xlearn#readme",
  "devDependencies": {
    "concurrently": "^8.2.2",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "ts-node-dev": "^2.0.0"
  }
}
